pipeline {
    agent { label 'agent1' }
 
    stages {
      
        stage('Run') {
            steps {
                dir('java_file') {
                    sh 'ls -l'
                    sh 'javac Main.java'
                    sh 'java Main'
                }
            }
        }
    }
}
