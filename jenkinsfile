pipeline {
    agent { "agent1"}
    stages {
        stage('Build') {
            steps {
                script {
                sh 'echo "hello"'
                }
            }
        }
    }
}
